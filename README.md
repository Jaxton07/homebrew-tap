# homebrew-tap

Homebrew tap for [pi-desktop](https://github.com/Jaxton07/pi-desktop).

```bash
brew install --cask jaxton07/tap/pi-desktop
# 升级
brew upgrade --cask pi-desktop
```

Cask 由 pi-desktop 的 release workflow 自动更新（tag 推送后渲染 `Casks/pi-desktop.rb`）。
